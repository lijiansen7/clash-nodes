name: Update Clash Nodes

on:
  schedule:
    # 每 6 小时自动更新一次 (UTC)
    - cron: '0 */6 * * *'
  workflow_dispatch: # 支持在 Actions 页面手动触发

permissions:
  contents: write # 允许把生成的订阅文件推送回仓库

jobs:
  update:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 安装依赖
        run: pip install requests pyyaml cryptography

      - name: 下载 mihomo 内核
        env:
          GH_PROXY: "" # CI 直连 GitHub, 不需要加速器
        run: python get_mihomo.py

      - name: 测试节点并生成订阅
        env:
          GH_PROXY: ""
        run: python clash_node_tester.py --engine mihomo --gh-proxy "" --export-proxy-ok mihomo_alive.yaml

      - name: 提交并推送订阅文件
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add mihomo_alive.yaml blocked_subs.txt
          git commit -m "Update nodes $(Get-Date -Format 'yyyy-MM-dd HH:mm')" || echo "No changes to commit"
          git push
